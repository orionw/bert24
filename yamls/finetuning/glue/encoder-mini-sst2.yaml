base_run_name: encoder-mini-sst2-seed-42
callbacks:
  early_stopper:
    metric_to_monitor: ${metric_to_monitor}
    task: ${task}
  lr_monitor: {}
  speed_monitor: {}
default_seed: 42
device_train_microbatch_size: 64
learning_rate: 5e-5
local_pretrain_checkpoint_folder: ./ft_sweeps/encoder-mini-sst2-pretrain-encoder-sst2
loggers:
  wandb:
    entity: mmarone-jhu
    project: sweeps
max_duration: 1
max_seq_len: 512
metric_to_monitor: MulticlassAccuracy
model:
  model_config:
    activation_function: gelu
    allow_embedding_resizing: true
    attention_layer: rope
    attention_probs_dropout_prob: 0.0
    attn_out_bias: false
    attn_out_dropout_prob: 0.1
    attn_qkv_bias: false
    bert_layer: prenorm
    causal_mask: false
    compile_model: true
    deterministic_fa2: false
    embed_dropout_prob: 0.0
    embed_norm: true
    embedding_layer: sans_pos
    final_norm: true
    global_attn_every_n_layers: 3
    head_class_act: gelu
    head_pred_act: gelu
    hidden_act: gelu
    hidden_size: 512
    init_method: full_megatron
    intermediate_size: 768
    local_attn_rotary_emb_base: 160000.0
    local_attn_rotary_emb_dim: null
    loss_function: fa_cross_entropy
    loss_kwargs:
      reduction: mean
    masked_prediction: true
    mlp_dropout_prob: 0.0
    mlp_in_bias: false
    mlp_layer: glu
    mlp_out_bias: false
    norm_kwargs:
      bias: false
      eps: 1.0e-05
    normalization: layernorm
    num_attention_heads: 8
    num_hidden_layers: 19
    pad_logits: false
    padding: unpadded
    rotary_emb_base: 160000.0
    rotary_emb_dim: null
    rotary_emb_interleaved: false
    rotary_emb_scale_base: null
    skip_first_prenorm: true
    sliding_window: 128
    unpad_embeddings: true
    use_fa2: true
    vocab_size: 50368
  name: flex_bert
  pretrained_checkpoint: ${starting_cp}
  pretrained_model_name: bert-base-uncased
  tokenizer_name: ${tokenizer_name}
  use_pretrained: false
optimizer:
  betas:
  - 0.9
  - 0.98
  eps: 1.0e-06
  filter_bias_norm_wd: true
  log_grad_norm: true
  lr: ${learning_rate}
  name: decoupled_adamw
  weight_decay: ${weight_decay}
parallel: false
precision: amp_bf16
save_finetune_checkpoint_folder: ${save_finetune_checkpoint_prefix}/${base_run_name}
save_finetune_checkpoint_prefix: ./ft_sweeps/encoder-mini-sst2-encoder-sst2
save_num_checkpoints_to_keep: 1
scheduler:
  alpha_f: 0.0
  name: linear_decay_with_warmup
  t_warmup: 0.06dur
starting_checkpoint_load_path: ${starting_cp}
starting_cp: /home/oweller2/my_scratch/retrieval_pretraining/bert24/models_pythia_like/encoder_mini_no_packing_prolong_decay_lower_mask/latest-rank0.pt
task: sst2
tasks:
  abc:
    seeds:
    - 42
    trainer_kwargs:
      batch_size: ${device_train_microbatch_size}
      max_duration: ${max_duration}
      max_sequence_length: ${max_seq_len}
      save_num_checkpoints_to_keep: ${save_num_checkpoints_to_keep}
      save_overwrite: true
tokenizer_name: bclavie/olmo_bert_template
weight_decay: 5e-6
